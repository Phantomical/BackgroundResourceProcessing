// In addition to the normal adapters for various modules, we also provide a
// set of modules that can be used to get custom behaviour on a case-by-case
// basis.
//
// This config file lists out definitions for those modules so that they are
// automatically picked up if they get used.

BACKGROUND_CONVERTER
{
    name = ModuleBackgroundConstantConverter
    adapter = ModuleAdapter
}
