// PersistentThrust Integration.

// There are multiple relevant versions of PersistentThrust floating around.
// Currently we only support 1.7.5, so key the patch off the integration binary.
BACKGROUND_CONVERTER:NEEDS[BackgroundResourceProcessing.Integration.PersistentThrust.17]
{
    name = PersistentEngine
    adapter = BackgroundPersistentEngine
}
