// SCANsat is pretty easy.
//
// It uses ModuleResourceHandler and has an obvious property to check to see
// if is active.

BACKGROUND_CONVERTER:NEEDS[SCANsat]
{
    name = SCANsat
    adapter = BackgroundGenericConverter

    ActiveCondition = %scanningNow
}
