// Kopernicus integration.
//
// We just replace the default solar panel adapter with one that properly
// handles multiple stars in kopernicus.

@BACKGROUND_CONVERTER:HAS[#adapter[BackgroundSolarPanel]]:FOR[BackgroundResourceProceessing]:NEEDS[Kopernicus]
{
    @adapter = BackgroundKopernicusSolarPanel
}

